---
plugins:
  api:
    plugins:
      assets.core:
        plugins:
          assets.claimrights:
            aliases: [claimRights1]
            use_default: True

config:
  auth:
    type: file
    src: auth.ini
  database:
    couchdb.db_name: tests
    couchdb.url: http://op:op@localhost:5984/
